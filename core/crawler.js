// (future) Puppeteer/Playwright implementation for heavy crawling
module.exports = {
    async crawl(url) {
        console.log(`Crawl requested for ${url} (Puppeteer not yet implemented)`);
        return null;
    }
};
